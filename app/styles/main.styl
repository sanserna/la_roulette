/**
 * CONTENIDOS
 *
 * FRAMEWORKS
 * normalize.css................Estandariza los estilos.
 * animate.css..................Colección de animaciones.
 * jquery.fullPage.css..........Transiciones entre las secciones de la pagina.
 *
 * BASE
 * _fonts.......................Configuracion de la fuente principal.
 * _base........................Estilos base en los elementos de la pagina.
 * _base_mixins.................Algunos mixins utiles.
 * _base_variables..............Declaracion de variables globales.
 *
 * COMPONENTES
 * .content.....................Contenedor principal.
 * #video-container.............Contenedor del video del landing page y sus elementos.
 *
 * COMPONENTES
 *
 */

// frameworks imports
@import 'frameworks/normalize'
//@import 'frameworks/animate.css'

// Base imports
@import 'base/_fonts'
@import 'base/_base'
@import 'base/_base_mixins'
@import 'base/_base_variables'

// Tus estilos van aqui
#video-container {
    min-height 100vh
    overflow hidden
    position relative
    max-height 100vh
    width 100%
    #video {
        height 100vh
        width 100%
    }
    .logo-bg {
        position absolute
        top 0
        left 0
        height 100%
        width 100%
        background rgba(black,.1) url(../img/pattern.png)
        z-index 0
    }
    .lr-logo {
        area()
        position absolute
        width 20%
        left 50%
        margin-left -35%
        top 75px
        z-index 1
    }
}

.page-content {
    height 1000px
    background rgba(red,.1)
}




















